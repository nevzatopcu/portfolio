import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Nevzat Topçu | Frontend Developer",
  description: "Portfolio website of Nevzat Topçu, a Frontend Developer specializing in Angular, React, and modern web technologies.",
  keywords: ["frontend developer", "angular", "react", "javascript", "typescript", "web development", "Nevzat Topçu"],
  authors: [{ name: "Nevzat Topçu" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
